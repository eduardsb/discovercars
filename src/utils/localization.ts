import LocalizedStrings from 'react-native-localization';
import English from '../assets/translations/en_en.json';

const strings = new LocalizedStrings({
    en: English,
});

export default strings;
